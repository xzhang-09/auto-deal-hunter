# LLM (MCP agent)
OPENAI_API_KEY=your-openai-api-key

# HuggingFace (datasets, models)
HF_TOKEN=your-huggingface-token
HF_USER=your-huggingface-username
LITE_MODE=true                    # true for T4/Colab; false for A100 full

# Pushover (push notifications for deals)
PUSHOVER_USER=your-pushover-user
PUSHOVER_TOKEN=your-pushover-token

# Specialist Agent (fine-tuned Qwen 2.5 on Modal)
USE_SPECIALIST=false
PRICER_DATASET=your-huggingface-username/pricer-data

# Fine-tuning (train_pricer.py, Colab)
# LITE_MODE is above. Override in Colab Secrets if needed.
